query Configs{
    getConfigs(id: "0x1"){
        id
        API_Token
        Client_ID
        expires_at
        access_token
        expires_in
        first_issued_at
        pull_LiveBroadcast
   }
}
